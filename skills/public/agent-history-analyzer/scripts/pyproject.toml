[project]
name = "agent-history-analyzer-scripts"
version = "0.1.0"
description = "Deterministic helper scripts for the Agent History Analyzer skill"
readme = ""
requires-python = ">=3.11"
dependencies = [
    "jieba>=0.42.1",
    "pandas>=2.2.0",
    "scikit-learn>=1.5.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
